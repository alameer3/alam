# YEMEN 🇾🇪 FLIX

منصة سينما يمنية متطورة لعرض الأفلام والمسلسلات العربية والأجنبية مع نظام GitHub Integration التلقائي.

## المميزات الرئيسية

### 🎬 نظام المحتوى
- عرض الأفلام والمسلسلات العربية والأجنبية
- نظام فلترة متقدم حسب النوع والفئة
- مشغل فيديو متطور مع إعدادات الجودة والسرعة
- نظام تقييمات ومراجعات المستخدمين

### 🔧 النظام التقني
- **Frontend**: React 18 + TypeScript + Tailwind CSS
- **Backend**: Node.js + Express + TypeScript
- **Database**: PostgreSQL + Drizzle ORM
- **UI Components**: Radix UI + shadcn/ui

### 🚀 GitHub Integration التلقائي
- مزامنة تلقائية للتعديلات كل دقيقة
- حفظ حالة المشروع تلقائياً
- استيراد سلس من GitHub إلى Replit
- تتبع تقدم التطوير

## التثبيت والتشغيل

### 🔄 الاستيراد من GitHub
```bash
# في Replit:
# 1. اختر "Import from GitHub"
# 2. أدخل رابط المشروع
# 3. سيتم التشغيل تلقائياً
```

### ⚡ البدء السريع
```bash
# تشغيل الإعداد التلقائي
node quick-start.js

# أو التشغيل المباشر
npm install
npm run dev
```

### 🔧 الإعداد اليدوي
```bash
# 1. استنساخ المشروع
git clone <repository-url>
cd yemen-flix

# 2. تثبيت التبعيات
npm install

# 3. إعداد البيئة
cp .env.example .env
# قم بتحديث إعدادات قاعدة البيانات

# 4. تشغيل المشروع
npm run dev
```

## نظام GitHub Integration

### 🔄 المزامنة التلقائية
```bash
# بدء المزامنة التلقائية
node github-auto-sync.js

# سيتم رفع التعديلات تلقائياً كل دقيقة
```

### 📋 حفظ حالة المشروع
- يحفظ تقدم التطوير في `.replit-project-state.json`
- يتتبع الميزات المكتملة والخطوات التالية
- يحفظ إعدادات قاعدة البيانات والخادم

### 🎯 الاستيراد الذكي
```bash
# عند الاستيراد من GitHub
node project-import-setup.js

# يتعرف تلقائياً على:
# - حالة المشروع
# - التبعيات المطلوبة
# - إعدادات قاعدة البيانات
# - تقدم التطوير
```

## الملفات المهمة

### 📄 ملفات النظام
- `.replit-project-state.json` - حالة المشروع
- `.replit-config.json` - إعدادات النظام
- `.local/state/replit/agent/progress_tracker.md` - تتبع التقدم

### 📚 أدلة التطوير
- `IMPORT_GUIDE.md` - دليل الاستيراد
- `replit.md` - توثيق المشروع
- `auto-setup.sh` - سكريبت الإعداد التلقائي

## الميزات المكتملة

### ✅ واجهة المستخدم
- واجهة متجاوبة باللغة العربية
- نظام ثيمات متعدد (فاتح/داكن/سينما)
- تصميم متجاوب للموبايل والتابلت

### ✅ نظام المحتوى
- إدارة الأفلام والمسلسلات
- فلترة متقدمة حسب النوع والفئة
- نظام بحث ذكي
- مشغل فيديو متطور

### ✅ النظام الإداري
- لوحة تحكم إدارية شاملة
- إدارة المستخدمين والصلاحيات
- نظام حماية متقدم
- مراقبة الأداء

### ✅ قاعدة البيانات
- PostgreSQL مع Drizzle ORM
- فهرسة محسنة للأداء
- نظام نسخ احتياطية
- تخزين مؤقت ذكي

## الاستخدام

### 🎬 للمستخدمين
```
الصفحة الرئيسية: http://localhost:5000
البحث: /search
الأفلام: /movies
المسلسلات: /series
```

### 🔧 للمطورين
```
لوحة التحكم: /unified-admin
API Documentation: /api/docs
حالة النظام: /api/health
```

## إعداد GitHub

### 1. إنشاء Token
```bash
# في GitHub:
# Settings → Developer settings → Personal access tokens
# إنشاء token مع صلاحيات repo
```

### 2. تحديث متغيرات البيئة
```bash
# في .env
GITHUB_TOKEN=your_token_here
GITHUB_REPO=username/yemen-flix
```

### 3. تفعيل المزامنة
```bash
# تشغيل المزامنة التلقائية
node github-auto-sync.js &
```

## استكشاف الأخطاء

### 🔍 مشاكل شائعة

**مشكلة**: لا تعمل قاعدة البيانات
```bash
# تحقق من DATABASE_URL في .env
# إنشاء قاعدة بيانات PostgreSQL في Replit
```

**مشكلة**: فشل المزامنة مع GitHub
```bash
# تحقق من GITHUB_TOKEN
# تأكد من صلاحيات repository
```

**مشكلة**: خطأ في المنفذ
```bash
# تأكد من أن المنفذ 5000 متاح
# أو غيّر PORT في .env
```

## المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء فرع جديد للميزة
3. Commit التغييرات
4. إنشاء Pull Request

## الترخيص

MIT License - راجع [LICENSE](LICENSE) للتفاصيل.

## الدعم

- 📧 للدعم التقني: راجع `IMPORT_GUIDE.md`
- 📋 للتطوير: راجع `replit.md`
- 🐛 للأخطاء: إنشاء Issue في GitHub

## آخر تحديث
${new Date().toLocaleString('ar-EG')}

---

تم تطوير هذا المشروع بـ ❤️ للمجتمع اليمني